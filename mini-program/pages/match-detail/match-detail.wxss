/* ============================================
   GOAL! 全屏庆祝(F-P0-2)
   触发瞬间:多巴胺爆发 + 球队色脉冲 + confetti 雨
   ============================================ */
.goal-overlay {
  position: fixed; inset: 0;
  z-index: 9999;
  display: flex; align-items: center; justify-content: center;
  background: rgba(0,0,0,.55);
  overflow: hidden;
  animation: fadeIn .25s ease-out;
}
@keyframes fadeIn { from { opacity: 0 } to { opacity: 1 } }

.goal-bg-pulse {
  position: absolute; inset: 0;
  opacity: .85;
  animation: bgPulse 1.2s ease-in-out infinite;
}
@keyframes bgPulse {
  0%, 100% { transform: scale(1); opacity: .8; }
  50% { transform: scale(1.08); opacity: .9; }
}

.confetti {
  position: absolute;
  top: -60rpx;
  border-radius: 4rpx;
  animation: confettiFall linear forwards;
  z-index: 1;
}
@keyframes confettiFall {
  0%   { transform: translateY(0) rotate(0deg);    opacity: 1; }
  60%  { opacity: 1; }
  100% { transform: translateY(140vh) rotate(720deg); opacity: 0; }
}

.goal-content {
  position: relative; z-index: 2;
  text-align: center;
  padding: 60rpx 40rpx;
  max-width: 90vw;
  animation: goalIn .55s cubic-bezier(.22,1.5,.36,1) both;
}
@keyframes goalIn {
  0%   { transform: scale(.4); opacity: 0; }
  60%  { transform: scale(1.12); opacity: 1; }
  100% { transform: scale(1); }
}

.goal-flag-row {
  margin-bottom: 24rpx;
  animation: flagWiggle .8s ease-in-out infinite;
}
@keyframes flagWiggle {
  0%, 100% { transform: rotate(-3deg); }
  50% { transform: rotate(3deg); }
}
.goal-flag-big {
  font-size: 180rpx;
  line-height: 1;
  filter: drop-shadow(0 8rpx 24rpx rgba(0,0,0,.4));
}

.goal-text {
  display: block;
  font-family: 'Bebas Neue', 'Helvetica Neue', sans-serif;
  font-size: 220rpx;
  font-weight: 900;
  letter-spacing: 6rpx;
  line-height: 1;
  text-shadow: 0 6rpx 24rpx rgba(0,0,0,.35);
  margin-bottom: 16rpx;
}
.goal-team {
  display: block;
  font-size: 56rpx;
  font-weight: 800;
  letter-spacing: 1rpx;
  margin-top: 12rpx;
  text-shadow: 0 3rpx 12rpx rgba(0,0,0,.3);
}
.goal-scorer {
  display: block;
  font-size: 28rpx;
  font-weight: 600;
  margin-top: 12rpx;
  letter-spacing: 1rpx;
}

.goal-actions {
  display: flex;
  gap: 16rpx;
  justify-content: center;
  margin-top: 48rpx;
}
.goal-actions .btn {
  min-width: 220rpx;
}

/* DEMO 触发按钮(开发期可见 · 生产环境隐藏)*/
.demo-triggers {
  display: flex; flex-direction: column; gap: 10rpx;
  margin-top: 20rpx;
}
.demo-trigger {
  padding: 14rpx 20rpx;
  background: rgba(20, 184, 166, .08);
  border: 2rpx dashed rgba(20, 184, 166, .4);
  border-radius: 14rpx;
  text-align: center;
}
.demo-trigger-tx {
  font-size: 22rpx; color: #0F766E; font-weight: 600;
  letter-spacing: .5rpx;
}

/* ============================================
   F-P0-6 胜利卡 overlay(可分享 · Beat KOL)
   ============================================ */
.card-overlay {
  position: fixed; inset: 0;
  z-index: 9997;
  background: rgba(0,0,0,.65);
  backdrop-filter: blur(8rpx);
  display: flex; align-items: center; justify-content: center;
  padding: 32rpx;
  animation: fadeIn .25s ease-out;
}
.vcard-frame {
  background: #FFFFFF;
  border-radius: 28rpx;
  padding: 32rpx 28rpx 28rpx;
  width: 100%;
  max-width: 640rpx;
  box-shadow: 0 32rpx 80rpx rgba(0,0,0,.5);
  animation: htCardIn .35s cubic-bezier(.22,1.4,.36,1) both;
}

/* Brand row */
.vcard-head {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 18rpx;
}
.vcard-brand-mark {
  width: 44rpx; height: 44rpx;
  border-radius: 50%;
  background: #FFFFFF;
  border: 3rpx solid #14B8A6;
  display: inline-flex; align-items: center; justify-content: center;
  font-family: 'Helvetica Neue'; font-weight: 800; font-size: 22rpx;
  color: #14B8A6;
}
.vcard-brand-tx {
  font-family: 'Bebas Neue', 'Helvetica Neue', sans-serif;
  font-size: 30rpx; font-weight: 900;
  letter-spacing: 3rpx; color: #1F2937;
}
.vcard-matchday {
  font-size: 20rpx; font-weight: 700; letter-spacing: 2rpx;
  color: #9CA3AF;
}

/* 类型 badge */
.vcard-badge {
  text-align: center;
  padding: 18rpx 24rpx;
  border-radius: 16rpx;
  font-family: 'Bebas Neue', sans-serif;
  font-size: 36rpx; font-weight: 900;
  letter-spacing: 3rpx;
  margin-bottom: 24rpx;
}
.vcard-badge-standard {
  background: linear-gradient(135deg, #ECFDF5, #FFFFFF);
  color: #0F766E;
}
.vcard-badge-beat_kol {
  background: linear-gradient(135deg, #6D5BFF, #8B7AFF);
  color: #FFFFFF;
  text-shadow: 0 2rpx 8rpx rgba(0,0,0,.2);
}
.vcard-badge-bold {
  background: linear-gradient(135deg, #FFD54A, #F0AB22);
  color: #1A1A0A;
}
.vcard-badge-duel {
  background: linear-gradient(135deg, #FF6B7D, #FF8FA3);
  color: #FFFFFF;
}

/* 球队 + 比分 banner */
.vcard-score-row {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  padding: 32rpx 24rpx;
  border-radius: 20rpx;
  margin-bottom: 24rpx;
  gap: 16rpx;
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,.12);
}
.vcard-flag {
  font-size: 96rpx;
  line-height: 1;
  margin-bottom: 8rpx;
  filter: drop-shadow(0 4rpx 8rpx rgba(0,0,0,.2));
}
.vcard-team-code {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 32rpx; font-weight: 800;
  letter-spacing: 2rpx;
  text-shadow: 0 2rpx 6rpx rgba(0,0,0,.2);
}
.vcard-score {
  font-family: 'Helvetica Neue', sans-serif;
  font-size: 88rpx; font-weight: 900;
  letter-spacing: -2rpx;
  text-shadow: 0 4rpx 12rpx rgba(0,0,0,.25);
}

/* 预测对比 */
.vcard-picks {
  padding: 16rpx 24rpx;
  background: #F4F6F5;
  border-radius: 14rpx;
  margin-bottom: 24rpx;
}
.vcard-pick-row {
  display: flex; align-items: center; gap: 14rpx;
  padding: 6rpx 0;
}
.vcard-pick-mark {
  font-family: 'Helvetica Neue', sans-serif;
  font-weight: 900;
  font-size: 32rpx;
  width: 40rpx; text-align: center;
}
.vcard-pick-hit .vcard-pick-mark { color: #14B8A6; }
.vcard-pick-miss .vcard-pick-mark { color: #FF6B7D; }
.vcard-pick-tx {
  font-size: 26rpx; font-weight: 600; color: #1F2937;
}
.vcard-pick-miss .vcard-pick-tx { color: #6B7280; text-decoration: line-through; }

/* EDS earned */
.vcard-eds-block {
  text-align: center;
  padding: 24rpx 0;
  margin-bottom: 16rpx;
}
.vcard-eds-tx {
  font-family: 'Helvetica Neue', sans-serif;
  font-size: 48rpx; font-weight: 900;
  color: #14B8A6;
  letter-spacing: -.5rpx;
}

.vcard-foot {
  text-align: center;
  padding-top: 16rpx;
  border-top: 2rpx dashed #E5E7EB;
  margin-bottom: 20rpx;
}

.vcard-actions {
  display: flex; gap: 12rpx;
}
.vcard-actions .btn { flex: 1; }

/* ============================================
   F-P0-3 半场重押 overlay
   ============================================ */
.ht-overlay {
  position: fixed; inset: 0;
  z-index: 9998;
  background: rgba(0,0,0,.6);
  backdrop-filter: blur(8rpx);
  display: flex; align-items: center; justify-content: center;
  padding: 32rpx;
  animation: fadeIn .25s ease-out;
}
.ht-card {
  background: #FFFFFF;
  border-radius: 32rpx;
  padding: 40rpx 36rpx 32rpx;
  width: 100%;
  max-width: 640rpx;
  box-shadow: 0 24rpx 64rpx rgba(0,0,0,.3);
  animation: htCardIn .35s cubic-bezier(.22,1.4,.36,1) both;
}
@keyframes htCardIn {
  0%   { transform: translateY(40rpx) scale(.96); opacity: 0; }
  100% { transform: translateY(0) scale(1); opacity: 1; }
}

.ht-head {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 8rpx;
}
.ht-title {
  font-family: 'Bebas Neue', 'Helvetica Neue', sans-serif;
  font-size: 44rpx; font-weight: 900;
  color: #14B8A6; letter-spacing: 2rpx;
}
.ht-meta {
  font-size: 26rpx; font-weight: 700; color: #1F2937;
  font-family: 'Helvetica Neue', sans-serif;
}
.ht-subtitle {
  font-size: 24rpx; color: #6B7280;
  display: block;
  margin-bottom: 24rpx;
}

/* 倒计时 */
.ht-countdown-row {
  display: flex; align-items: center; gap: 16rpx;
  margin-bottom: 28rpx;
}
.ht-countdown-bar {
  flex: 1; height: 12rpx;
  background: #F3F4F6;
  border-radius: 6rpx; overflow: hidden;
}
.ht-countdown-fill {
  height: 100%;
  background: linear-gradient(90deg, #14B8A6, #5EEAD4);
  transition: width .98s linear;
  border-radius: 6rpx;
}
.ht-countdown-sec {
  font-family: 'JetBrains Mono', ui-monospace, Menlo, monospace;
  font-size: 28rpx; font-weight: 800; color: #14B8A6;
  min-width: 60rpx; text-align: right;
}

/* 5 个比分选项 */
.ht-options {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14rpx;
  margin-bottom: 20rpx;
}
.ht-opt {
  position: relative;
  background: #F4F6F5;
  border: 4rpx solid transparent;
  border-radius: 18rpx;
  padding: 24rpx 0;
  text-align: center;
  transition: all .15s;
}
.ht-opt.selected {
  background: #ECFDF5;
  border-color: #14B8A6;
  box-shadow: 0 6rpx 18rpx rgba(20, 184, 166, .2);
}
.ht-opt-label {
  font-family: 'Helvetica Neue', sans-serif;
  font-size: 36rpx; font-weight: 800;
  color: #1F2937;
  letter-spacing: -.5rpx;
}
.ht-opt.selected .ht-opt-label {
  color: #0F766E;
}
.ht-opt-friends {
  position: absolute; top: 6rpx; right: 10rpx;
  font-size: 18rpx; font-weight: 700; color: #14B8A6;
  background: rgba(20, 184, 166, .12);
  padding: 2rpx 8rpx; border-radius: 8rpx;
  font-family: ui-monospace, Menlo, monospace;
}

/* 社交反馈 */
.ht-social {
  text-align: center;
  padding: 12rpx 0;
}

/* Actions */
.ht-actions {
  display: flex; gap: 12rpx;
  margin-top: 12rpx;
}
.ht-actions .btn { flex: 1; }
.ht-actions .btn.is-disabled {
  opacity: 0.45; box-shadow: none;
}

/* 比赛详情 · 大比分 hero(参考截图 3:Brazil 2 : 4 Italy)*/
.detail-hero {
  padding: 32rpx 28rpx;
  margin-bottom: 24rpx;
}
.detail-comp {
  font-size: 22rpx; color: #9CA3AF;
  text-transform: uppercase; letter-spacing: 1rpx;
  text-align: center;
  margin-bottom: 20rpx;
}
.hero-vs-row {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  gap: 12rpx;
}
.hero-team {
  display: flex; flex-direction: column; align-items: center; gap: 12rpx;
}
.hero-team-name { font-size: 22rpx; color: #6B7280; font-weight: 600; }
.hero-team-code {
  font-size: 30rpx; font-weight: 800; color: #111827; letter-spacing: 1rpx;
}
.hero-vs-meta {
  display: flex; flex-direction: column; align-items: center; gap: 8rpx;
}
.hero-vs-tx {
  font-size: 60rpx; font-weight: 800;
  color: #111827; letter-spacing: 2rpx;
  font-family: 'Helvetica Neue', sans-serif;
}
.hero-countdown {
  font-size: 22rpx; font-weight: 700; color: #14B8A6;
  background: #ECFDF5;
  padding: 6rpx 14rpx; border-radius: 12rpx;
  font-family: ui-monospace, Menlo, monospace;
}
.hero-kickoff {
  font-size: 20rpx; color: #9CA3AF;
}
.hero-venue {
  margin-top: 20rpx;
  font-size: 22rpx; color: #9CA3AF;
  text-align: center;
}

/* ===== Pick row ===== */
.pick-row {
  display: flex; flex-direction: row; gap: 14rpx;
}
.pick {
  flex: 1;
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 26rpx 14rpx;
  display: flex; flex-direction: column; align-items: center; gap: 10rpx;
  border: 4rpx solid transparent;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,.03);
  transition: all .15s;
}
.pick.selected {
  border-color: #14B8A6;
  background: #F0FDFA;
  box-shadow: 0 6rpx 20rpx rgba(20, 184, 166, .15);
}
.pick-team {
  font-size: 26rpx; font-weight: 700; color: #1F2937;
  margin-top: 4rpx;
}
.pick.selected .pick-team { color: #0F766E; }
.pick-pct { font-size: 20rpx; color: #9CA3AF; }

.confirm-banner {
  background: #14B8A6;
  color: #fff;
  display: flex; align-items: center; justify-content: space-between; gap: 16rpx;
  padding: 18rpx 26rpx;
  margin-top: 20rpx;
  border-radius: 16rpx;
  box-shadow: 0 6rpx 20rpx rgba(20, 184, 166, .25);
}
.confirm-banner .tx-teal { color: #fff; }

/* ===== KOL row ===== */
.kol-row {
  display: flex; align-items: center; gap: 16rpx;
  padding: 18rpx 22rpx;
}
.kol-pick-pill {
  background: #ECFDF5;
  padding: 12rpx 20rpx;
  border-radius: 26rpx;
}

/* ============================================
   F-P0-4 Bold Call + 进阶预测
   ============================================ */
.bold-call {
  margin-top: 20rpx;
  background: linear-gradient(135deg, #FFFBEB, #FFFFFF);
  border: 2rpx solid rgba(255, 213, 74, .4);
  border-radius: 20rpx;
  padding: 22rpx 24rpx;
  transition: all .2s;
}
.bold-call.is-disabled {
  background: #F9FAFB;
  border-color: #E5E7EB;
  opacity: .65;
}
.bold-call-label {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 30rpx;
  font-weight: 800;
  color: #92400E;
  letter-spacing: 1rpx;
}
.bold-call.is-disabled .bold-call-label { color: #6B7280; }

.adv-section {
  margin-top: 20rpx;
}
.adv-toggle-row {
  display: flex; align-items: center; justify-content: space-between;
  padding: 18rpx 4rpx;
}
.adv-chevron {
  font-size: 36rpx;
  color: #14B8A6;
  transition: transform .2s;
}
.adv-chevron.is-open { transform: rotate(180deg); }

.adv-panel {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 24rpx 22rpx;
  box-shadow: 0 2rpx 12rpx rgba(15, 23, 42, .04);
  animation: bubbleIn .25s ease-out;
}

.adv-block { margin-bottom: 24rpx; }
.adv-block:last-of-type { margin-bottom: 0; }
.adv-block-title {
  display: block;
  font-size: 22rpx; color: #6B7280; font-weight: 600;
  text-transform: uppercase; letter-spacing: 1rpx;
  margin-bottom: 12rpx;
}

.adv-grid { display: grid; gap: 10rpx; }
.adv-grid-3 { grid-template-columns: repeat(3, 1fr); }
.adv-grid-6 { grid-template-columns: repeat(3, 1fr); }
@media (min-width: 480px) {
  .adv-grid-6 { grid-template-columns: repeat(6, 1fr); }
}

.adv-cell {
  background: #F4F6F5;
  border: 3rpx solid transparent;
  border-radius: 14rpx;
  padding: 18rpx 8rpx;
  text-align: center;
  font-size: 24rpx; font-weight: 700;
  color: #1F2937;
  font-family: 'Helvetica Neue', sans-serif;
  transition: all .15s;
}
.adv-cell.selected {
  background: #ECFDF5;
  border-color: #14B8A6;
  color: #0F766E;
}

.adv-judgment {
  margin-top: 24rpx;
  padding-top: 20rpx;
  border-top: 2rpx dashed #E5E7EB;
  display: flex; justify-content: space-between; align-items: center;
}
.adv-judgment-val {
  font-family: 'JetBrains Mono', ui-monospace, Menlo, monospace;
  font-size: 32rpx; font-weight: 800;
  color: #14B8A6;
  letter-spacing: -.5rpx;
}

/* ============================================
   F-P0-1 实时聊天室
   ============================================ */
.chat-section { margin-top: 36rpx; }

/* LIVE dot pulse · 红色实时感 */
.live-dot {
  display: inline-block;
  width: 12rpx; height: 12rpx;
  border-radius: 50%;
  background: #FF6B7D;
  margin-right: 8rpx;
  animation: liveDot 1.4s ease-in-out infinite;
  box-shadow: 0 0 8rpx #FF6B7D;
  vertical-align: 2rpx;
}
@keyframes liveDot {
  0%, 100% { opacity: 1; transform: scale(1); }
  50%      { opacity: 0.4; transform: scale(0.85); }
}

/* 聊天滚动区 · 固定高度 + 内滚动 */
.chat-scroll {
  background: #FFFFFF;
  border-radius: 24rpx;
  padding: 16rpx 12rpx;
  height: 720rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(15, 23, 42, .04);
}
.chat-empty {
  height: 100%;
  display: flex; align-items: center; justify-content: center;
}

/* 单条 bubble */
.chat-bubble {
  display: flex; align-items: flex-start; gap: 12rpx;
  padding: 10rpx 14rpx;
  margin-bottom: 8rpx;
  animation: bubbleIn .3s ease-out;
}
@keyframes bubbleIn {
  0%   { opacity: 0; transform: translateY(8rpx); }
  100% { opacity: 1; transform: translateY(0); }
}

.chat-bubble.is-mine {
  flex-direction: row-reverse;
}

.chat-bubble-body {
  flex: 1; min-width: 0;
  background: #F4F6F5;
  border-radius: 16rpx;
  padding: 12rpx 16rpx;
  border-left: 4rpx solid transparent;
  max-width: 78%;
}

.chat-bubble.is-mine .chat-bubble-body {
  background: #14B8A6;
  color: #FFFFFF;
  border-left: 0;
  border-right: 4rpx solid rgba(255,255,255,.4);
}

.chat-bubble.is-my-camp .chat-bubble-body {
  background: #ECFDF5;
}

.chat-bubble.is-opponent .chat-bubble-body {
  background: #FFF1F2;
}

.chat-user-name {
  font-size: 22rpx; font-weight: 700; color: #1F2937;
}
.chat-bubble.is-mine .chat-user-name { color: #FFFFFF; }

.chat-text {
  display: block;
  font-size: 26rpx; color: #1F2937;
  line-height: 1.45;
  word-break: break-word;
}
.chat-bubble.is-mine .chat-text { color: #FFFFFF; }

/* Reaction bar */
.chat-reactions {
  display: flex; gap: 10rpx;
  padding: 12rpx 0;
  overflow-x: auto;
}
.reaction-btn {
  flex-shrink: 0;
  width: 76rpx; height: 76rpx;
  border-radius: 50%;
  background: #FFFFFF;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: 36rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,.04);
  transition: transform .12s;
}
.reaction-btn:active {
  transform: scale(.88);
  background: #ECFDF5;
}

/* 输入栏 */
.chat-input-row {
  display: flex; gap: 12rpx; align-items: center;
  background: #FFFFFF;
  border-radius: 28rpx;
  padding: 8rpx 8rpx 8rpx 24rpx;
  box-shadow: 0 2rpx 10rpx rgba(15, 23, 42, .05);
}
.chat-input {
  flex: 1;
  min-height: 56rpx;
  font-size: 26rpx;
  color: #1F2937;
  background: transparent;
}
.chat-input-locked {
  padding: 14rpx 16rpx;
}
.chat-send-btn {
  flex-shrink: 0;
  min-width: 100rpx;
  height: 56rpx;
  border-radius: 28rpx;
  padding: 0 22rpx;
  background: #E5E7EB;
  color: #9CA3AF;
  font-size: 24rpx;
  font-weight: 700;
  border: 0;
  display: inline-flex; align-items: center; justify-content: center;
}
.chat-send-btn.is-ready {
  background: #14B8A6;
  color: #FFFFFF;
}
.chat-send-btn[disabled] {
  opacity: 1; /* 我们自定 disabled 样式不要默认半透明 */
}
